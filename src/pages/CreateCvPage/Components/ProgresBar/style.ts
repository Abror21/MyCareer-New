import styled from 'styled-components';

export const StyledProgresBarCreateCV = styled.div`
  .progres-bar {
    background-color: white;
    border-radius: 20px;
    padding: 40px;
    margin-top: 25px;

    .ant-progress-bg {
      background-color: var(--base-color);
    }

    h2 {
      font-family: var(--primary-font);
      font-size: 1.1111rem;
      font-style: normal;
      font-weight: 700;
      line-height: normal;
      margin: 5px 0;
    }
  }
`;
